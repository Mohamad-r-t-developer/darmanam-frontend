export type WoundValues = {
  woundLength: number;
  needSupplies: boolean;
  supplyDetails: string;
};

export type BurnValues = {
  area: string[];
  needSupplies: boolean;
  supplyDetails: string;
};

export type SutureValues = {
  area: string[];
};

export type InjectionValues = {
  count: number;
 
};

export type SerumInjectionValues = {
  ampouleCount: number;
};
